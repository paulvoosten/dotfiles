#
# WM independent hotkeys
#

# Terminal emulator
super + Return
    termite -t Termite

# Dropdown terminal emulator
super + shift + Return
    [ ! -s /tmp/scratchid ] && exit;\
    id=$(cat /tmp/scratchid);\
    bspc node $id --flag hidden;bspc node -f $id

# Program launcher
super + e
    rofi -modi 'window,run,drun,ssh' -show drun

alt + Tab
    rofi -modi 'window' -show window -theme windows

super + s
    jgmenu_run >/dev/null 2>&1 &

# Make sxhkd reload its configuration files:
super + Escape
    pkill -USR1 -x sxhkd; notify-send -a 'sxhkd' 'Reloaded config'

# Take screenshot of focused window/whole screen(s)
{_,shift + }Print
    scrot -{u,m} "$HOME/pictures/screenshots/%Y-%m-%d %H:%M:%S.png"; notify-send -a 'Screenshot' 'A new screenshot has been made'

# Multimedia
XF86Audio{Play,Stop,Next,Prev,RaiseVolume,LowerVolume}
    playerctl {play-pause,stop,next,previous,volume 0.05+,volume 0.05-}
#    mpc {toggle,stop,next,prev,volume +5,volume -5}

# Session menu
super + x
    pgrep -x oblogout || oblogout

